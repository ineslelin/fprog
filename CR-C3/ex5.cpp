// SKIPPED

/*
    Example program that reads a text file and checks if it's in EBCDIC format. Please use the Maybe monad for error management.
    1. First, define the Maybe type alias, which uses std::optional:
    2. Next, create a readFile lambda function to read the contents of a file:
    3. Create a  isEBCDIC lambda function to check if the file content is EBCDIC:
    4. Define the processFileContent lambda function to process the file content:
    5. Inside the main function, set the filename and call the processFileContent lambda function:
    6. Finally, display the file content if it's in EBCDIC format, or an error message if it's not:
*/